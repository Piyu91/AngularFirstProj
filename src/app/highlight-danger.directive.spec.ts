import { HighlightDangerDirective } from './highlight-danger.directive';

describe('HighlightDangerDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDangerDirective();
    expect(directive).toBeTruthy();
  });
});
